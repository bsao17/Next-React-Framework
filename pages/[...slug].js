import Navigation from "../components/Navigation"

const Slug = () => {
    return (
        <div style={{textAlign: "center"}}>
            <h1 style={{color: "red"}}>Slug</h1>
        </div>
    )
}

export default Slug